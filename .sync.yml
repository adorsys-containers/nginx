.github/workflows/ci.yaml:
  checkoutTestApplications: true
  env:
    - DOCKER_IMAGE: adorsys/nginx
      TAG: centos
      DIR: centos
      SQUASH: 'true'
      ALIASES: 'latest'
    - DOCKER_IMAGE: adorsys/nginx
      TAG: alpine
      DIR: alpine
      SQUASH: 'true'
    - DOCKER_IMAGE: adorsys/nginx
      TAG: ubi
      DIR: ubi
      SQUASH: 'true'
